import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.taviswhite.KappaConnect',
  appName: 'KappaConnect',
  webDir: 'dist',
};

export default config;
